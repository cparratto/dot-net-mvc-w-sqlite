---
version: '2'
services:
  common:
    build: "."
    environment:
      NODE_ENV: production
  app:
    extends:
      service: common
    links:
    - db
    ports:
    - 5000:5000
    command: dotnet run
